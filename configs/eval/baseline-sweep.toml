# Model is supplied by the operator with `prime eval run ... -m MODEL`.
rollouts_per_example = 3

[[eval]]
id = "commonground-predict"
num_examples = 20

[[eval]]
id = "commonground-elicit"
num_examples = 20
