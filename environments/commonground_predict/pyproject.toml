[project]
name = "commonground-predict"
version = "0.3.0"
description = "Probabilistic masked-vote prediction over synthetic stakeholder panels."
authors = [{ name = "Common Ground contributors" }]
license = "Apache-2.0 AND MPL-2.0"
license-files = ["LICENSE", "LICENSES/MPL-2.0.txt", "NOTICE"]
readme = "README.md"
requires-python = ">=3.12,<3.13"
keywords = ["social-reasoning", "prediction", "deterministic", "single-turn"]
tags = ["social-reasoning", "vote-prediction", "deterministic", "single-turn", "train", "eval"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "commonground-scenarios==0.2.0",
    "commonground-score==0.2.0",
    "datasets>=5.0.1,<6.0.0",
    "verifiers==0.3.0",
]

[project.urls]
Repository = "https://github.com/doctor-gonzo/commonground-envs"

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = [
    "commonground_predict/**",
]

[tool.verifiers.eval]
num_examples = 100
rollouts_per_example = 1
